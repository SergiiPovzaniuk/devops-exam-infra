global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    email_configs:
      - to: sergii.povzaniuk@gmail.com
        from: alertmanager@localhost
        smarthost: localhost:25
        require_tls: false
        send_resolved: true